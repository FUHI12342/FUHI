# 実装履歴

要件定義書の管理表(§5)は「今どうなっているか」だけを載せる。「いつ何をしたか」はここに残す。

## 2026-09-01

| PR | 内容 |
|---|---|
| #1 | 要件定義、Phase 0(Django 5.2 化・Secrets・TZバグ・二重予約制約・祝日動的化)、Phase 1(営業日/チェックリスト/開店シグナル、シフト/打刻/CSV、座席/座席ボード/ウォークイン)、Phase 2(SNS下書き→承認→配信、Threads/X/Instagram アダプタ、Pillow 画像)、Phase 3(商品/入出庫/発注案→承認→送信、GBP 条件付き)、CI、Dockerfile、seed_demo、敵対的コードレビュー10指摘の全件対応 |
| #2 | 店長ロール(承認系操作の権限分離)、PostgreSQL(DATABASE_URL)、Sentry、本番HTTPS設定、運用想定ドキュメント。セキュリティレビュー指摘0件 |
| #3 | 深夜営業対応(閉店30時まで、25時・26時枠、GBP日跨ぎ)、開店処理の排他制御 |
| #11 | 店舗ごとの機能フラグ(SNS/在庫/座席ボード/GBP)、「Web予約のみ」方針の確定、残タスクの Issue 起票(#4〜#10) |
| #12 | 全体リファクタリング(挙動変更なし): 共通アクセス制御層 `booking/access.py`、時刻スロット層 `booking/timeslots.py`、5アプリのビューを統一、settings の env ヘルパー化、booking テストのパッケージ分割、資料再構成(本ファイル新設) |
| — | Issue #4 顧客向けWeb座席予約(飲食業態・ログイン不要): 人数×日付→時間帯ごとの空き座席候補→連絡先入力→確定。`Schedule.staff` を null 許可にし座席予約を `staff=None, seat=座席` で既存の座席重複制約に載せる。確認メール(取消URL付き)、開始時刻まで顧客自身でキャンセル可、営業日の臨時営業時間を反映、深夜枠対応、フラグ `enable_web_reservation`(飲食業態のみ)。テスト +24(計146) |

### 改修ラウンドで対応したレビュー指摘(PR #1 内)

| # | 指摘 | 対応 |
|---|---|---|
| 1 | SNS配信が AdapterError 以外の例外で中断し再試行不能 | 例外全捕捉+失敗記録、承認済み下書きの再配信(`retry_unfinished`) |
| 2 | 深夜跨ぎ・24時超の営業時間でカレンダーが500 | DB制約で遮断 → PR #3 で本対応 |
| 3 | 発注済み商品が永久に再提案されない | 入荷済みステータス+30日自動解除 |
| 4 | 承認済みの日に再生成すると同日二重投稿 | (store, date) UNIQUE+承認済みは再生成不可 |
| 5 | DEBUG デフォルト true・SECRET_KEY フォールバックでフェイルオープン | DEBUG=false 時は開発用キーで起動拒否、Docker に DEBUG=false 焼き込み |
| 6 | ウォークイン着席の競合 | 部分UNIQUE制約 |
| 7 | 店舗メンバー判定の重複定義 | `booking/access.py` に一本化 |
| 8 | 在庫一覧の N+1 | annotate で1クエリ化 |
| 9 | Pillow>=10.0 でフォールバックが TypeError | Pillow>=10.1 にピン |
| 10 | 勤怠CSVの N+1 | 当月分を一括ロード |
